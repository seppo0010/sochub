import { Trello } from '../types/TrelloPowerUp';

const GoogleDocs = {
    icon: '',
    text: 'Import from Google Docs',
    callback: (t: Trello.PowerUp.IFrame) => Promise.resolve(),
}
export default GoogleDocs;
